export class Menu {
    
    menuZone;

    constructor() {
        this.menuZone = document.querySelector('#game-menu');
    }

    /**
     * Ajoute le launcher du jeu dans le menu de jeu
     * 
     * @param {Instance d'un objet jeu} gameObject
     */
    setMenu(gameObject) {
        
    }
}